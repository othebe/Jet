// This holds initial data for Jet.

module Jet.Application.InitialData {
    export module Board {
        // Board dimensions.
        export var dimensions = {
            top: 0,
            left: 0,
            width: 150,
            height: 100
        };

        // Zoom.
        export var zoom = 100;
    }
} 